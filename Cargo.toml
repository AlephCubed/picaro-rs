[package]
name = "picaro-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
rand_chacha = { version = "0.9.0", default-features = false, features = [
  "os_rng",
] }

[dev-dependencies]
criterion = { version = "0.7" }

[[bench]]
name = "encrypt"
harness = false

[[bench]]
name = "decrypt"
harness = false

[[bench]]
name = "s_box"
harness = false
